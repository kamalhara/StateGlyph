{
  "name": "@stateicons/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "stateicons": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stateicons/core": "0.0.0",
    "commander": "^15.0.0"
  }
}
